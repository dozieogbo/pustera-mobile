<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Pustera"
             x:Class="Pustera.MainPage">
    <Grid 
        VerticalOptions="Fill" 
        HorizontalOptions="Fill">
        <!--<ContentView
            HeightRequest="20"
            Padding="5"
               BackgroundColor="Gray" 
               x:Name="Loader">
            <Label
               HorizontalOptions="CenterAndExpand" 
               TextColor="White" 
               FontAttributes="Bold"  
               Text="Loading..." 
               FontSize="Micro" 
               />
        </ContentView>-->

        <WebView 
            x:Name="MWebView" 
            HeightRequest="1000" 
            WidthRequest="1000" 
            VerticalOptions="Fill" 
            HorizontalOptions="Fill"
            IsVisible="True"
            Opacity="0.25"
            />
        
        <ActivityIndicator
            VerticalOptions="Center"
            HorizontalOptions="Center"
            IsRunning="True"
            IsVisible="True"
            Color="#090945"
            x:Name="MLoader"/>
    </Grid>
</ContentPage>
